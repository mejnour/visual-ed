#:kivy 1.10.0
#:import FadeTransition kivy.uix.screenmanager.FadeTransition

ScreenManagement:
    MainScreen:

<Button>:
    font_size: 20
    size_hint: None, None
    size: 150, 40

<MainWidget>:
    GridLayout:
        rows: 2
        row_force_default: 0
        rows_minimum: {0: self.height * .45, 1: self.height * .55}
        spacing: 5
        width: 300
        height: 500
        # row_default_height: 500

        Label:
            # canvas:
            #     Color:
            #         rgba: 0, 1, 0, 0.8
            #
            #     Rectangle:
            #         pos: self.pos
            #         size: self.size

            markup: 1
            text: '[b]Visual[/b]ED'
            font_size: 35
            size_hint: 1, 0.4
            pos_hint: {'center_x': 1, 'y': 50}

        BoxLayout:
            orientation: 'vertical'
            spacing: 5

            # canvas:
            #     Color:
            #         rgba: 0, 1, 0, 0.8
            #
            #     Rectangle:
            #         pos: self.pos
            #         size: self.size

            Button:
                text: 'Pilha'
                pos_hint: {'center_x': .5, 'center_y': 1}
                on_release: app.root.current = 'pilha'

            Button:
                text: 'Fila'
                pos_hint: {'center_x': .5, 'center_y': .75}

            Button:
                text: 'Lista'
                pos_hint: {'center_x': .5, 'center_y': 5}

            Button:
                text: 'LSE'
                pos_hint: {'center_x': .5, 'center_y': .25}

            Button:
                text: 'ABP'
                pos_hint: {'center_x': .5, 'center_y': .25}

            Widget:
                size_hint: None, None
                size: 100, 40

<MainScreen>:
    GridLayout:
        cols: 2
        col_force_default: 0
        cols_minimum: {0: self.width * .3, 1: self.width * .7}
        # col_default_width: 300
        spacing: 5
        height: self.height
        # width: 400


        MainWidget:
            background_color: gray

        FloatLayout:
            # canvas:
            #     Color:
            #         rgba: 1, 0, 0, 0.8
            #
            #     Rectangle:
            #         pos: self.pos
            #         size: self.size



            Label:
                markup: 1
                text: 'A Visual\n[b]Data Structures[/b]\nRepresentation'
                font_size: 40
                size_hint: 1, 0.4
                pos_hint: {'center_x': 0.5, 'center_y': 0.5}

            ScreenManagement:
                transition: FadeTransition()
                Pilha:
                Fila:
                Lista:
                LSE:

            Pilha:
                name: 'pilha'
                # canvas:
                #     Color:
                #         rgba: 1, 0, 0, 0.8
                #
                #     Rectangle:
                #         pos: self.pos
                #         size: self.size
            #
            # Fila:
            #     name: 'fila'
            #     canvas:
            #         Color:
            #             rgba: 0, 0, 1, 0.8
            #
            #         Rectangle:
            #             pos: self.pos
            #             size: self.size
            #
            # Lista:
            #     name: 'lista'
            #
            # LSE:
            #     name: 'lse'
